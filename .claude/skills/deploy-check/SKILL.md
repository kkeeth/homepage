# deploy-check

デプロイ前の最終チェックを一括実行する。

## Steps

1. `pnpm lint` で ESLint チェックを実行
2. `pnpm ts` で TypeScript 型チェックを実行
3. `pnpm build` でプロダクションビルドを実行
4. 各ステップの結果をまとめてレポート

## Usage

デプロイ前や PR 作成前にこのスキルを実行して、ビルド・lint・型チェックがすべてパスすることを確認する。

## Output

各チェックの成功/失敗を一覧表示し、失敗があればエラー内容を表示する。
