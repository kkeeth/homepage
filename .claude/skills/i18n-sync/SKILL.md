# i18n-sync

`ja.json` と `en.json` の翻訳キーの同期状態をチェックする。

## Steps

1. `src/locales/ja.json` と `src/locales/en.json` を読み込む
2. 両ファイルのキーをフラットに展開して比較する
3. 片方にしか存在しないキーを検出する
4. 結果をレポートする

## Usage

UI テキストの追加・変更後にこのスキルを実行して、翻訳漏れがないことを確認する。

## Output

- 同期済み: 「すべてのキーが同期されています」
- 差分あり: 片方にしか存在しないキーの一覧を表示
